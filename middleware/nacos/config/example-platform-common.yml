spring:
  main:
    banner-mode: off
  mvc:
    problemdetails:
      enabled: true
app:
  logging:
    file-enabled: false
logging:
  level:
    com.example: info
    org.springframework.jdbc.core.JdbcTemplate: info
