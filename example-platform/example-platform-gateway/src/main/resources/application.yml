spring:
  application:
    name: example-platform-gateway
  profiles:
    active: @active@
  config:
    import:
      - optional:nacos:example-platform-common.yml
      - optional:nacos:example-platform-gateway.yml
  cloud:
    nacos:
      server-addr: localhost:8848
      username: nacos
      password: nacos
      namespace: dev
